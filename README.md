# LanAudioGateway Gateway Core

Android AAR for LAN audio devices. This repository serves as the public project page for the SDK's Maven dependency; the Android source project and signing credentials are not stored here.

The release artifact will be published under the Maven Central group `io.github.zwb-zale` with artifact ID `gateway-core`. Use the version shown on Maven Central after the first release is validated.
